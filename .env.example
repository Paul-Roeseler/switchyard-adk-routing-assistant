# Short-lived Google Cloud OAuth token used by the Vertex AI targets.
# Generate with: gcloud auth application-default print-access-token
VERTEX_ACCESS_TOKEN=

# NVIDIA Inference Hub key used by the classifier, simple, and medium targets.
INFERENCE_HUB_API=

# Google Cloud project used for document embeddings. Authentication uses ADC.
GOOGLE_CLOUD_PROJECT=model-routing-505414
GOOGLE_CLOUD_LOCATION=global

# The ADK model client calls this local Switchyard OpenAI endpoint.
SWITCHYARD_BASE_URL=http://127.0.0.1:4000/v1
